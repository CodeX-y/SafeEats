<%= form_with(model: ingredient) do |form| %>
  <div>
    <label for="ingredient_name" style="display: block">Ingredient Name</label>
    <input type="text" name="ingredient_name" id="ingredient_name"/>
  </div>

  <div>
    <input type="submit" value="Add Ingredient"/>
  </div>
<% end %>
